import styled from 'styled-components';

export const PokeDescription = styled.div` 
    margin-bottom: 40px;

    h6 {
        text-transform: uppercase;
        margin-bottom: 20px;
        color: #333;
        font-weight: bold;
    }
`